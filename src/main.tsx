import React from 'react';
import ReactDOM from 'react-dom/client';
import WrongnessPortfolioApp from './wrongness-portfolio-app.tsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WrongnessPortfolioApp />
  </React.StrictMode>,
);